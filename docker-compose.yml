services:
  postgres:
    image: postgres:16

    container_name: college-postgres

    environment:
      POSTGRES_DB: college_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123

    ports:
      - "5432:5432"